DATABASE_URL=postgresql+asyncpg://user:password@localhost/drive_db
DB_NAME=drive_db
DB_USER=user
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=5432
STORAGE_ROOT=/srv/cloud-data

# Production ENV storage root, but other above details are only for db container.